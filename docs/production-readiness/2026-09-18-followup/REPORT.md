# Исправления в main —18сентября2026

**Google Play: NO-GO. App Store: NO-GO.** Работа продолжена непосредственно в main по прямому указанию владельца. Production не изменялся. Исходный аудит и предыдущий отчёт сохранены без изменений.

- Из17 исходных дефектов: **8 FIXED & VERIFIED**, **3 IMPLEMENTED, NOT VERIFIED**, **6 BLOCKED**, **0 OPEN**. Из11 исходных release blockers закрыты **4**, остаются7. Карта D10 теперь исправлена в web, но native acceptance не выполнен — её не засчитываем закрытой целиком.
- Новых дефектов всего **6 (D18–D23)**, все6 исправлены и проверены указанным в реестре методом. В этом продолжении найдены и исправлены D22 (ложное наличие) и D23 (повторное списание награды после потерянного ответа). Новых незакрытых подтверждённых дефектов0; это не утверждение об отсутствии дефектов в непроверенной части.
- [Полный реестр](CLOSURE.md) сохраняет исходное поведение, acceptance, компоненты, коммиты и доказательства всех17 исходных и6 новых ID. Оставшиеся интеграционные/юридические/native пробелы включены в оценку и [действия владельца](OWNER_ACTIONS.md).

## Реальные изменения

Предыдущие исправления безопасности/заказов/бонусов/удаления перенесены из ветки исправлений в main fast-forward. Убрано повышение роли по телефону; сервер проверяет action/account/phone-bound OTP, TTL/attempts/replay, DB-роль, токены и admin endpoints. Заказ проверяет доверенную цену/остаток/филиал и сохраняет идемпотентный результат. История бонусов читает полный журнал, возвраты и параллельные операции проверены. Полное описание и границы — [предыдущий отчёт](../2026-09-18-remediation/REPORT.md). Реальная SMS/inventory/media инфраструктура всё ещё не подключена.

Дополнительно в main:

1. **Каталог —1291e02.** Обнаружен и изучен общий Bazaar backend; его контракт — product pages со строковыми/множественными категориями, без отдельного categories route и id-filter. Новый server gateway собирает полный каталог, выводит категории, фильтрует точный ID/вторичную категорию, проверяет полноту и неизменность контекста. Запрещены клиентские org/store параметры. Single-flight cache60с; не более3 upstream запросов одновременно. На настоящих данных:2365 SKU,24 GET,6.207с; категории и точный товар корректны. D22: `unavailable` больше не совпадает с `available`, отрицательные остатки имеют приоритет над флагом наличия. Поддержаны imageObjects и пустые image arrays.
2. **Карта —a2178f3.** Вместо жёсткого iframe popup385px — Leaflet/OSM с проверенными2GIS координатами6 филиалов, ограниченной шириной popup, адаптивной высотой260–390, доступными zoom и атрибуцией. Сохранены selector, карточки, внешние2GIS ссылки, timeout/retry. Реальные снимки320/390/430, отказ сети и восстановление подтверждены. Замена провайдера явно отражена в [MAP_SOURCES](MAP_SOURCES.md); native и геолокация внешнего приложения не засчитаны по web.
3. **Бонусные награды —615510d,9a7b421.** Раньше каждое подтверждение создавало новый ключ, что после потерянного ответа давало два списания. Теперь ключ/награда привязаны к аккаунту и сохранены до запроса; неизвестный результат восстанавливается тем же ключом даже после reload/смены аккаунта. Принятый результат хранится до явного «Понятно». Нулевая доступность после первого списания не блокирует восстановление. При удалении аккаунта локальная попытка удаляется. Серверный окончательный отказ не маскируется успехом. Статусы переведены на русский.

Общий Bazaar repo f64a7ce изучался только для совместимости и не менялся. Его наличие устраняет прежнюю неизвестность об исходниках, но не даёт staging mapping/резервов/POS. Все новые продуктовые изменения — в одном мобильном репозитории. [Порядок внедрения, backup/rollback и совместимость](DEPLOYMENT.md).

## Проверки и сборки

- Повторно прошли **135 прежних регрессионных проверок**: безопасность40, заказы28, loyalty14, удаление25, media6, каталог22. Реальная изолированная PostgreSQL на127.0.0.1:55448; внешние SMS/Telegram/storage заменены явно тестовыми адаптерами.
- Новый каталог:12 групп отрицательных/положительных сценариев; отдельно реальные публичные данные без записей. Новый reward recovery:11 сценариев модуля/БД, включая старое воспроизведение; **20 параллельных повторов → одна заявка и одна запись списания**.
- **10 web-состояний** карты/каталога и **6 web/API проверок** награды. Потеря201 принудительно воспроизведена после COMMIT; reload и повтор дали тот же результат, однаDBоперация и один debit3000 в тестовом аккаунте. Смена аккаунта не раскрыла квитанцию.
- TypeScript и lint PASS. Финальный **Android Hermes + iOS Hermes + web export PASS**. Это не standalone native release.
- Прежние Android engineering APK/AAB из5b8ea84 сохранены в `artifacts/remediation-20260918/android/`; подпись test/debug, endpoint inert. **Новые изменения в них отсутствуют.** Текущая main не объявляется проверенной этой сборкой. Нынешняя среда: нет JDK/установленного AndroidSDK, sdkmanager launcher есть; Xcode недоступен, только CommandLineTools. Новая самостоятельная APK/AAB/IPA не собрана. Физических устройств нет по ответу владельца.

[Логи/команды/ограничения](TEST_RUN.md), [скриншоты до/после](GALLERY.html), [матрица39 экранов](SCREEN_MATRIX.csv), [manifest исходников и сборок](evidence/builds.json). Старые доказательства унаследованы явно, не представлены как новый прогон всех экранов.

## Готовность и покрытие

Формулы и веса12 направлений не изменены. Все147 исходных ID/expected/method/weight сохранены. Основная расширенная матрица151 строка включает4 ранее добавленных внешних gates и ставший применимым OTP; новых критериев в этом продолжении нет. Замена средства проверки web карты раскрыта явно, native требования не ослаблены.

|Платформа|Baseline,147: R/C|Теперь,те же147: R/C|Теперь,151: R/C|
|---|---:|---:|---:|
|Общая|18.35% /36.29%|37.46% /41.32%|35.88% /39.66%|
|Android|21.01% /38.54%|42.65% /44.93%|40.49% /42.68%|
|iOS|21.01% /38.37%|42.01% /44.36%|39.86% /42.11%|

Предыдущие151:33.43% готовности/39.66% покрытия; сейчас35.88%/39.66%. CORE-06 и UI-07 перешли FAIL→PASS, поэтому готовность выросла, а покрытие этого этапа не выросло. Повтор зелёных проверок не приносит проценты. [Все12 направлений отдельно для ALL/Android/iOS](COMPARISON.md), [расчёт](CALCULATION.md), [CSV](CRITERIA.csv).

**80% не достигнуты.** Даже если все нефизические строки исходной матрицы станут PASS, без58 обязательных physical-device сценариев математический максимум51.41%. Это не достигнутый результат и не обещание51.41% сейчас. Удалять эти требования или засчитывать написанный код как выполненные испытания оснований нет.

## Оставшееся

Нужны утверждённые бизнес-/правовые тексты, заполненный release ассортимент, staging inventory/POS/SMS/media/Telegram и сверка legacy admin/sessions. Нужны подписи, Xcode и реальные устройства, native/network/privacy доказательства, консольные анкеты и review credentials. Q05 о UGC/report/block/moderator остаётся неразрешённым вопросом исходного аудита; admin hide не закрывает весь store gate. Точные действия без повторного запроса уже полученных исходников/ключа карты — [OWNER_ACTIONS](OWNER_ACTIONS.md). [Оба store checklist сверены с официальными источниками18.09.2026](STORE_CHECKLISTS.md). Публикация и production rollout не выполнялись.
