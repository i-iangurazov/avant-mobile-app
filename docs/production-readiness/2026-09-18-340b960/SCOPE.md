# Охват и история

2026-09-18: аудит текущего dirty working tree поверх 340b960; manifest фиксирует точные файлы. Продуктовый код не изменяется. Статусы присваиваются после создания CRITERIA.csv. Нативные Android/iOS критерии оцениваются независимо. Веб проверяет конкретные общие компоненты, но не закрывает native acceptance. Backend запускается в отдельном кластере PostgreSQL, исходники копируются без .env. Внешние записи и Telegram отправки не разрешены.

Уточнение после инвентаризации, до первого расчёта: добавлены CORE-11, REL-06, LOY-17, UI-07/08 (только WEB), API-08, A11Y-04/05. Причина: отдельные ранее не сформулированные свойства контрактов, истории, карты, контраста. Исходные веса не менялись. Готовность native UI не получает web PASS. Всего147 критериев.

При итоговом расчёте UX-02/03/04 и CORE-10 явно ограничены WEB и исключены из Android/iOS: browser evidence не выдаётся за общий native PASS. Изменений весов ради результата нет.
